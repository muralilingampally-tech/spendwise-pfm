import React, { useEffect } from "react"; import { TrendingDown, TrendingUp, AlertTriangle } from "lucide-react"; import { useForm } from "react-hook-form"; import { useApp } from "../../context/AppContext"; import { Modal } from "../common/Modal"; import { Button } from "../common/Button"; import { Transaction, TransactionType, TransactionFormData, NavigationTab } from "../../types"; export interface TransactionModalProps { isOpen: boolean; onClose: () => void; initialData?: Transaction | null; } export function TransactionModal({ isOpen, onClose, initialData = null }: TransactionModalProps) { const { categoryGroups, paymentSources, addTransaction, updateTransaction, setCurrentView } = useApp(); const { register, handleSubmit, watch, setValue, reset, formState: { errors } } = useForm<TransactionFormData>({ defaultValues: { type: TransactionType.EXPENSE, date: new Date().toISOString().split("T")[0], amount: undefined, groupId: "", paymentSourceId: "", remarks: "" } }); const selectedType = watch("type"); useEffect(() => { if (initialData && isOpen) { reset({ type: initialData.type, date: initialData.date.toDate().toISOString().split("T")[0], amount: initialData.amount, groupId: initialData.groupId, paymentSourceId: initialData.paymentSourceId, remarks: initialData.remarks || "" }); } else if (isOpen) { reset({ type: TransactionType.EXPENSE, date: new Date().toISOString().split("T")[0], amount: undefined, groupId: "", paymentSourceId: "", remarks: "" }); } }, [initialData, isOpen, reset]); const activeGroups = categoryGroups.filter(g => g.type === selectedType); const onSubmit = async (data: TransactionFormData) => { if (initialData?.id) { await updateTransaction(initialData.id, data); } else { await addTransaction(data); } onClose(); }; const isMasterDataEmpty = activeGroups.length === 0 || paymentSources.length === 0; return ( <Modal isOpen={isOpen} onClose={onClose} title={initialData ? "Edit Transaction" : "New Transaction"}> {isMasterDataEmpty ? ( <div className="text-center space-y-4 py-4"> <div className="w-12 h-12 bg-amber-100 text-amber-600 rounded-full flex items-center justify-center mx-auto"> <AlertTriangle className="w-6 h-6" /> </div> <p className="text-sm text-slate-500">Please create Categories and Payment Sources first.</p> <Button className="w-full" onClick={() => { onClose(); setCurrentView(NavigationTab.MASTERS); }}> Go to Master Data </Button> </div> ) : ( <form onSubmit={handleSubmit(onSubmit)} className="space-y-4"> <div className="grid grid-cols-2 gap-2 p-1 bg-slate-100 dark:bg-slate-800 rounded-xl"> <button type="button" onClick={() => { setValue("type", TransactionType.EXPENSE); setValue("groupId", ""); }} className={`py-2 rounded-lg text-sm font-semibold flex items-center justify-center gap-2 ${selectedType === TransactionType.EXPENSE ? "bg-rose-500 text-white" : "text-slate-600 hover:text-slate-900"}`}> <TrendingDown className="w-4 h-4" /> Expense </button> <button type="button" onClick={() => { setValue("type", TransactionType.INCOME); setValue("groupId", ""); }} className={`py-2 rounded-lg text-sm font-semibold flex items-center justify-center gap-2 ${selectedType === TransactionType.INCOME ? "bg-emerald-500 text-white" : "text-slate-600 hover:text-slate-900"}`}> <TrendingUp className="w-4 h-4" /> Income </button> </div> <div className="grid grid-cols-1 sm:grid-cols-2 gap-4"> <div> <label className="block text-xs font-semibold text-slate-600 dark:text-slate-400 mb-1">Amount (?) *</label> <input type="number" step="0.01" {...register("amount", { required: "Amount is required", min: { value: 0.01, message: "Must be > 0" } })} className="w-full px-3 py-2 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl font-semibold focus:ring-2 focus:ring-emerald-500 outline-none" /> {errors.amount && <p className="text-xs text-rose-500 mt-1">{errors.amount.message}</p>} </div> <div> <label className="block text-xs font-semibold text-slate-600 dark:text-slate-400 mb-1">Date *</label> <input type="date" {...register("date", { required: "Date is required" })} className="w-full px-3 py-2 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl focus:ring-2 focus:ring-emerald-500 outline-none" /> {errors.date && <p className="text-xs text-rose-500 mt-1">{errors.date.message}</p>} </div> </div> <div> <label className="block text-xs font-semibold text-slate-600 dark:text-slate-400 mb-1">Category *</label> <select {...register("groupId", { required: "Category is required" })} className="w-full px-3 py-2 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl focus:ring-2 focus:ring-emerald-500 outline-none"> <option value="">Select Category</option> {activeGroups.map(g => <option key={g.id} value={g.id}>{g.name}</option>)} </select> {errors.groupId && <p className="text-xs text-rose-500 mt-1">{errors.groupId.message}</p>} </div> <div> <label className="block text-xs font-semibold text-slate-600 dark:text-slate-400 mb-1">Payment Source *</label> <select {...register("paymentSourceId", { required: "Source is required" })} className="w-full px-3 py-2 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl focus:ring-2 focus:ring-emerald-500 outline-none"> <option value="">Select Source</option> {paymentSources.map(p => <option key={p.id} value={p.id}>{p.name}</option>)} </select> {errors.paymentSourceId && <p className="text-xs text-rose-500 mt-1">{errors.paymentSourceId.message}</p>} </div> <div> <label className="block text-xs font-semibold text-slate-600 dark:text-slate-400 mb-1">Remarks</label> <textarea rows={2} {...register("remarks")} placeholder="Notes..." className="w-full px-3 py-2 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl text-sm focus:ring-2 focus:ring-emerald-500 outline-none" /> </div> <div className="flex justify-end gap-3 pt-2"> <Button type="button" variant="outline" onClick={onClose}>Cancel</Button> <Button type="submit" variant="primary">{initialData ? "Save Changes" : "Add Transaction"}</Button> </div> </form> )} </Modal> ); }
