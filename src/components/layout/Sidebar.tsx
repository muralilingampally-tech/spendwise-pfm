import React from "react"; import { Wallet, BarChart3, ArrowRightLeft, PieChart as PieChartIcon, Layers, Settings as SettingsIcon, X, Users, User } from "lucide-react"; import { useApp } from "../../context/AppContext"; import { NavigationTab, UserRole } from "../../types"; export function Sidebar({ mobileOpen, setMobileOpen }: { mobileOpen: boolean; setMobileOpen: (open: boolean) => void }) { const { currentView, setCurrentView, userProfile, updateUserProfile } = useApp(); const navItems: { id: NavigationTab; label: string; icon: React.ElementType }[] = [ { id: "dashboard" as NavigationTab, label: "Dashboard", icon: BarChart3 }, { id: "transactions" as NavigationTab, label: "Transactions", icon: ArrowRightLeft }, { id: "reports" as NavigationTab, label: "Reports & Analytics", icon: PieChartIcon }, { id: "masters" as NavigationTab, label: "Master Data", icon: Layers }, { id: "settings" as NavigationTab, label: "Settings", icon: SettingsIcon }, ]; const handleNavClick = (id: NavigationTab) => { setCurrentView(id); setMobileOpen(false); }; return ( <> {mobileOpen && ( <div className="fixed inset-0 bg-slate-900/60 z-30 lg:hidden backdrop-blur-sm transition-opacity" onClick={() => setMobileOpen(false)} /> )} <aside className={`fixed top-0 bottom-0 left-0 z-40 w-64 bg-white dark:bg-slate-900 border-r border-slate-200 dark:border-slate-800 flex flex-col transition-transform duration-300 ease-in-out lg:translate-x-0 ${mobileOpen ? "translate-x-0" : "-translate-x-full"}`}> <div className="p-6 border-b border-slate-100 dark:border-slate-800 flex items-center justify-between"> <div className="flex items-center gap-3"> <div className="w-10 h-10 rounded-xl bg-gradient-to-tr from-emerald-500 to-teal-600 flex items-center justify-center text-white shadow-md shadow-emerald-500/20"> <Wallet className="w-6 h-6" /> </div> <div> <h1 className="font-extrabold text-xl tracking-tight text-slate-900 dark:text-white">SpendWise</h1> <p className="text-xs font-medium text-emerald-600 dark:text-emerald-400">Multi-User PFM Engine</p> </div> </div> <button className="lg:hidden p-1.5 rounded-lg text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-800" onClick={() => setMobileOpen(false)}> <X className="w-5 h-5" /> </button> </div> <div className="p-4 mx-4 my-4 rounded-xl bg-slate-50 dark:bg-slate-800/60 border border-slate-100 dark:border-slate-800"> <div className="flex items-center justify-between mb-2"> <span className="text-xs font-bold uppercase tracking-wider text-slate-400 dark:text-slate-500 flex items-center gap-1"> <Users className="w-3 h-3" /> Family Space </span> <span className={`text-[10px] font-bold px-2 py-0.5 rounded-full ${ userProfile.role === "Admin" ? "bg-amber-100 text-amber-800 dark:bg-amber-900/40 dark:text-amber-300" : userProfile.role === "Member" ? "bg-blue-100 text-blue-800 dark:bg-blue-900/40 dark:text-blue-300" : "bg-slate-200 text-slate-700 dark:bg-slate-700 dark:text-slate-300" }`}> {userProfile.role} </span> </div> <div className="font-semibold text-slate-800 dark:text-slate-200 text-sm truncate">{userProfile.familyName}</div> <div className="text-xs text-slate-500 dark:text-slate-400 truncate flex items-center gap-1 mt-0.5"> <User className="w-3.5 h-3.5" /> {userProfile.name} </div> </div> <nav className="flex-1 px-4 space-y-1 overflow-y-auto py-2"> {navItems.map((item) => { const Icon = item.icon; const isActive = currentView === item.id; return ( <button key={item.id} onClick={() => handleNavClick(item.id)} className={`w-full flex items-center gap-3 px-4 py-3 rounded-xl text-sm font-semibold transition-all duration-200 ${isActive ? "bg-emerald-500 text-white shadow-lg shadow-emerald-500/25 dark:bg-emerald-600" : "text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800 hover:text-slate-900 dark:hover:text-slate-100"}`} > <Icon className={`w-5 h-5 ${isActive ? "text-white" : "text-slate-400 dark:text-slate-500"}`} /> <span>{item.label}</span> </button> ); })} </nav> <div className="p-4 border-t border-slate-100 dark:border-slate-800"> <label className="block text-[11px] font-semibold text-slate-400 mb-1.5 uppercase tracking-wider">Role Switcher</label> <div className="grid grid-cols-3 gap-1 bg-slate-100 dark:bg-slate-800 p-1 rounded-lg text-xs"> {(["Admin", "Member", "Viewer"] as UserRole[]).map((role) => ( <button key={role} onClick={() => updateUserProfile({ role })} className={`py-1 rounded font-medium transition-colors ${ userProfile.role === role ? "bg-white dark:bg-slate-700 text-slate-900 dark:text-white shadow-sm" : "text-slate-500 hover:text-slate-900 dark:hover:text-white" }`} > {role} </button> ))} </div> </div> </aside> </> ); }
