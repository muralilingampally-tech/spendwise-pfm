import React from "react"; import { Menu, Plus, Moon, Sun } from "lucide-react"; import { useApp } from "../../context/AppContext"; import { Button } from "../common/Button"; export function TopNav({ setMobileOpen, onOpenAddTransaction }: { setMobileOpen: (open: boolean) => void; onOpenAddTransaction: () => void }) { const { theme, toggleTheme, userProfile } = useApp(); return ( <header className="sticky top-0 z-20 bg-white/80 dark:bg-slate-900/80 backdrop-blur-md border-b border-slate-200/80 dark:border-slate-800/80 px-4 lg:px-8 py-3 flex items-center justify-between"> <div className="flex items-center gap-3"> <button onClick={() => setMobileOpen(true)} className="lg:hidden p-2 rounded-xl text-slate-600 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800" > <Menu className="w-6 h-6" /> </button> <div> <h2 className="text-lg font-bold text-slate-900 dark:text-white leading-tight">Overview</h2> <p className="text-xs text-slate-500 dark:text-slate-400 hidden sm:block">Family: {userProfile.familyName} • Signed in as {userProfile.name}</p> </div> </div> <div className="flex items-center gap-3"> {userProfile.role !== "Viewer" && ( <Button onClick={onOpenAddTransaction} icon={Plus}> <span className="hidden sm:inline">Add Record</span> </Button> )} <button onClick={toggleTheme} className="p-2.5 rounded-xl border border-slate-200 dark:border-slate-800 text-slate-600 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors" title="Toggle Dark/Light Mode" > {theme === "light" ? <Moon className="w-4 h-4" /> : <Sun className="w-4 h-4 text-amber-400" />} </button> </div> </header> ); }
