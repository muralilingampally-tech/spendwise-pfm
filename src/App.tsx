import React, { useState, useEffect, useMemo, createContext, useContext } from "react"; import { Wallet, TrendingUp, TrendingDown, DollarSign, Layers, PieChart as PieChartIcon, BarChart3, Settings as SettingsIcon, Plus, Search, Edit2, Trash2, Download, Printer, Moon, Sun, CheckCircle, XCircle, ArrowRightLeft, X, CreditCard, ShieldCheck, User, Menu, Users, AlertTriangle } from "lucide-react"; export type UserRole = "Admin" | "Member" | "Viewer"; export interface UserProfile { uid: string; name: string; email: string; familyId: string; familyName: string; role: UserRole; } export type MasterType = "EXPENSE_GROUP" | "EXPENSE_SUBGROUP" | "INCOME_GROUP" | "INCOME_SUBGROUP" | "PAYMENT_SOURCE"; export interface MasterItem { id?: string; familyId: string; masterType: MasterType; masterName: string; name?: string; parentId?: string; groupName?: string; description?: string; type?: string; accountNumber?: string; displayOrder?: number; isActive?: boolean; } export type TransactionType = "income" | "expense"; export interface Transaction { id?: string; familyId: string; type: TransactionType; date: string; groupId: string; groupName: string; subGroupId?: string; subGroupName?: string; paymentSourceId: string; paymentSourceName: string; amount: number; remarks?: string; createdBy: string; createdOn: string; updatedBy: string; updatedOn: string; isDeleted: boolean; status: "active" | "archived"; } export interface ToastMessage { message: string; type: "success" | "error" | "info"; } export type NavigationTab = "dashboard" | "transactions" | "reports" | "masters" | "settings"; export interface AppContextType { userProfile: UserProfile; setUserProfile: React.Dispatch<React.SetStateAction<UserProfile>>; theme: "light" | "dark"; toggleTheme: () => void; currentView: NavigationTab; setCurrentView: (view: NavigationTab) => void; toast: ToastMessage | null; showToast: (message: string, type?: "success" | "error" | "info") => void; transactions: Transaction[]; masters: MasterItem[]; expenseGroups: MasterItem[]; expenseSubGroups: MasterItem[]; incomeGroups: MasterItem[]; incomeSubGroups: MasterItem[]; paymentSources: MasterItem[]; isDataLoading: boolean; addTransaction: (data: Omit<Transaction, "id" | "familyId" | "createdBy" | "createdOn" | "updatedBy" | "updatedOn" | "isDeleted" | "status">) => void; updateTransaction: (id: string, data: Partial<Transaction>) => void; deleteTransaction: (id: string) => void; addMasterItem: (data: Omit<MasterItem, "id" | "familyId" | "isActive">) => void; deleteMasterItem: (id: string) => void; updateUserProfile: (data: Partial<UserProfile>) => void; } const AppContext = createContext<AppContextType | null>(null); export const useApp = () => { const context = useContext(AppContext); if (!context) throw new Error("useApp must be used within an AppProvider"); return context; }; export function AppProvider({ children }: { children: React.ReactNode }) { const [userProfile, setUserProfile] = useState<UserProfile>({ uid: "user-001", name: "Gatla Family Member", email: "user@spendwise.com", familyId: "fam_default", familyName: "Gatla Workspace", role: "Admin" }); const [theme, setTheme] = useState<"light" | "dark">("light"); const [currentView, setCurrentView] = useState<NavigationTab>("dashboard"); const [toast, setToast] = useState<ToastMessage | null>(null); const [transactions, setTransactions] = useState<Transaction[]>([ { id: "tx-1", familyId: "fam_default", type: "expense", date: new Date().toISOString().split("T")[0], groupId: "g1", groupName: "Groceries", paymentSourceId: "p1", paymentSourceName: "HDFC Bank", amount: 3450, remarks: "Weekly supermarket shopping", createdBy: "Gatla Member", createdOn: new Date().toISOString(), updatedBy: "Gatla Member", updatedOn: new Date().toISOString(), isDeleted: false, status: "active" }, { id: "tx-2", familyId: "fam_default", type: "income", date: new Date().toISOString().split("T")[0], groupId: "g2", groupName: "Salary", paymentSourceId: "p1", paymentSourceName: "HDFC Bank", amount: 85000, remarks: "Monthly salary credit", createdBy: "Gatla Member", createdOn: new Date().toISOString(), updatedBy: "Gatla Member", updatedOn: new Date().toISOString(), isDeleted: false, status: "active" } ]); const [masters, setMasters] = useState<MasterItem[]>([ { id: "m1", familyId: "fam_default", masterType: "EXPENSE_GROUP", masterName: "Groceries", name: "Groceries", isActive: true }, { id: "m2", familyId: "fam_default", masterType: "EXPENSE_GROUP", masterName: "Utilities", name: "Utilities", isActive: true }, { id: "m3", familyId: "fam_default", masterType: "INCOME_GROUP", masterName: "Salary", name: "Salary", isActive: true }, { id: "m4", familyId: "fam_default", masterType: "PAYMENT_SOURCE", masterName: "HDFC Bank", name: "HDFC Bank", type: "Bank", isActive: true }, { id: "m5", familyId: "fam_default", masterType: "PAYMENT_SOURCE", masterName: "Cash", name: "Cash", type: "Cash", isActive: true } ]); const showToast = (message: string, type: "success" | "error" | "info" = "success") => { setToast({ message, type }); setTimeout(() => setToast(null), 3500); }; const toggleTheme = () => { const nextTheme = theme === "light" ? "dark" : "light"; setTheme(nextTheme); if (nextTheme === "dark") { document.documentElement.classList.add("dark"); } else { document.documentElement.classList.remove("dark"); } }; const expenseGroups = useMemo(() => masters.filter(m => m.masterType === "EXPENSE_GROUP"), [masters]); const expenseSubGroups = useMemo(() => masters.filter(m => m.masterType === "EXPENSE_SUBGROUP"), [masters]); const incomeGroups = useMemo(() => masters.filter(m => m.masterType === "INCOME_GROUP"), [masters]); const incomeSubGroups = useMemo(() => masters.filter(m => m.masterType === "INCOME_SUBGROUP"), [masters]); const paymentSources = useMemo(() => masters.filter(m => m.masterType === "PAYMENT_SOURCE"), [masters]); const addTransaction = (data: Omit<Transaction, "id" | "familyId" | "createdBy" | "createdOn" | "updatedBy" | "updatedOn" | "isDeleted" | "status">) => { const now = new Date().toISOString(); const newTx: Transaction = { ...data, id: `tx-${Date.now()}`, familyId: userProfile.familyId, createdBy: userProfile.name, createdOn: now, updatedBy: userProfile.name, updatedOn: now, isDeleted: false, status: "active" }; setTransactions(prev => [newTx, ...prev]); showToast("Transaction added successfully!"); }; const updateTransaction = (id: string, data: Partial<Transaction>) => { setTransactions(prev => prev.map(t => t.id === id ? { ...t, ...data } : t)); showToast("Transaction updated!"); }; const deleteTransaction = (id: string) => { setTransactions(prev => prev.filter(t => t.id !== id)); showToast("Transaction deleted!"); }; const addMasterItem = (data: Omit<MasterItem, "id" | "familyId" | "isActive">) => { const newMaster: MasterItem = { ...data, id: `m-${Date.now()}`, familyId: userProfile.familyId, masterName: data.name || data.masterName, isActive: true }; setMasters(prev => [...prev, newMaster]); showToast("Master record added!"); }; const deleteMasterItem = (id: string) => { setMasters(prev => prev.filter(m => m.id !== id)); showToast("Master item deleted!"); }; const updateUserProfile = (data: Partial<UserProfile>) => { setUserProfile(prev => ({ ...prev, ...data })); showToast("Profile updated!"); }; return ( <AppContext.Provider value={{ userProfile, setUserProfile, theme, toggleTheme, currentView, setCurrentView, toast, showToast, transactions, masters, expenseGroups, expenseSubGroups, incomeGroups, incomeSubGroups, paymentSources, isDataLoading: false, addTransaction, updateTransaction, deleteTransaction, addMasterItem, deleteMasterItem, updateUserProfile }}> {children} </AppContext.Provider> ); } export default function App() { return ( <AppProvider> <MainLayout /> </AppProvider> ); } function MainLayout() { const { currentView, theme, toggleTheme, userProfile, toast } = useApp(); const [mobileOpen, setMobileOpen] = useState(false); const [isModalOpen, setIsModalOpen] = useState(false); return ( <div className={`min-h-screen ${theme === "dark" ? "dark bg-slate-950 text-slate-100" : "bg-slate-50 text-slate-900"} font-sans antialiased flex`}> {toast && ( <div className="fixed bottom-5 right-5 z-50 flex items-center gap-2 px-4 py-3 bg-slate-900 text-white dark:bg-slate-100 dark:text-slate-900 rounded-xl shadow-2xl animate-bounce"> <CheckCircle className="w-5 h-5 text-emerald-400" /> <span className="text-sm font-semibold">{toast.message}</span> </div> )} <aside className={`fixed top-0 bottom-0 left-0 z-40 w-64 bg-white dark:bg-slate-900 border-r border-slate-200 dark:border-slate-800 flex flex-col transition-transform ${mobileOpen ? "translate-x-0" : "-translate-x-full lg:translate-x-0"}`}> <div className="p-6 border-b border-slate-100 dark:border-slate-800 flex items-center justify-between"> <div className="flex items-center gap-3"> <div className="w-10 h-10 rounded-xl bg-gradient-to-tr from-emerald-500 to-teal-600 flex items-center justify-center text-white shadow-md"> <Wallet className="w-6 h-6" /> </div> <div> <h1 className="font-extrabold text-xl text-slate-900 dark:text-white">SpendWise</h1> <p className="text-xs text-emerald-600 dark:text-emerald-400 font-semibold">Personal Finance</p> </div> </div> </div> <div className="p-4 mx-4 my-4 rounded-xl bg-slate-50 dark:bg-slate-800/60 border border-slate-100 dark:border-slate-800"> <div className="flex items-center justify-between mb-1"> <span className="text-xs font-bold text-slate-400 uppercase flex items-center gap-1"><Users className="w-3 h-3"/> Workspace</span> <span className="text-[10px] font-bold px-2 py-0.5 rounded-full bg-emerald-100 dark:bg-emerald-900/40 text-emerald-700 dark:text-emerald-300">{userProfile.role}</span> </div> <div className="font-bold text-sm text-slate-800 dark:text-slate-200">{userProfile.familyName}</div> </div> <nav className="flex-1 px-4 space-y-1"> <NavItem id="dashboard" label="Dashboard" icon={BarChart3} /> <NavItem id="transactions" label="Transactions" icon={ArrowRightLeft} /> <NavItem id="reports" label="Reports" icon={PieChartIcon} /> <NavItem id="masters" label="Master Data" icon={Layers} /> <NavItem id="settings" label="Settings" icon={SettingsIcon} /> </nav> </aside> <div className="flex-1 lg:pl-64 flex flex-col min-w-0"> <header className="sticky top-0 z-20 bg-white/80 dark:bg-slate-900/80 backdrop-blur-md border-b border-slate-200 dark:border-slate-800 px-6 py-4 flex items-center justify-between"> <div className="flex items-center gap-3"> <button onClick={() => setMobileOpen(!mobileOpen)} className="lg:hidden p-2 rounded-xl border border-slate-200 dark:border-slate-800"><Menu className="w-5 h-5"/></button> <h2 className="text-lg font-bold">Welcome back, {userProfile.name}</h2> </div> <button onClick={toggleTheme} className="p-2.5 rounded-xl border border-slate-200 dark:border-slate-800 hover:bg-slate-100 dark:hover:bg-slate-800"> {theme === "light" ? <Moon className="w-4 h-4" /> : <Sun className="w-4 h-4 text-amber-400" />} </button> </header> <main className="p-6 max-w-7xl w-full mx-auto space-y-6"> {currentView === "dashboard" && <DashboardView onOpenAdd={() => setIsModalOpen(true)} />} {currentView === "transactions" && <TransactionsView />} {currentView === "reports" && <ReportsView />} {currentView === "masters" && <MastersView />} {currentView === "settings" && <SettingsView />} </main> </div> <QuickAddModal isOpen={isModalOpen} onClose={() => setIsModalOpen(false)} /> </div> ); } function NavItem({ id, label, icon: Icon }: { id: NavigationTab; label: string; icon: any }) { const { currentView, setCurrentView } = useApp(); const isActive = currentView === id; return ( <button onClick={() => setCurrentView(id)} className={`w-full flex items-center gap-3 px-4 py-3 rounded-xl text-sm font-semibold transition-all ${ isActive ? "bg-emerald-500 text-white shadow-md" : "text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800" }`} > <Icon className="w-5 h-5" /> <span>{label}</span> </button> ); } function DashboardView({ onOpenAdd }: { onOpenAdd: () => void }) { const { transactions } = useApp(); const income = transactions.filter(t => t.type === "income").reduce((acc, t) => acc + t.amount, 0); const expense = transactions.filter(t => t.type === "expense").reduce((acc, t) => acc + t.amount, 0); return ( <div className="space-y-6"> <div className="grid grid-cols-1 sm:grid-cols-3 gap-4"> <div className="p-5 bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm"> <span className="text-xs font-bold text-slate-400 uppercase">Total Income</span> <div className="text-2xl font-black text-emerald-600 dark:text-emerald-400 mt-2">?{income.toLocaleString("en-IN")}</div> </div> <div className="p-5 bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm"> <span className="text-xs font-bold text-slate-400 uppercase">Total Expense</span> <div className="text-2xl font-black text-rose-600 dark:text-rose-400 mt-2">?{expense.toLocaleString("en-IN")}</div> </div> <div className="p-5 bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm"> <span className="text-xs font-bold text-slate-400 uppercase">Net Surplus</span> <div className="text-2xl font-black text-indigo-600 dark:text-indigo-400 mt-2">?{(income - expense).toLocaleString("en-IN")}</div> </div> </div> <div className="p-6 bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 flex justify-between items-center"> <div> <h3 className="font-bold text-base">Quick Actions</h3> <p className="text-xs text-slate-400">Record income or expense transactions in seconds</p> </div> <button onClick={onOpenAdd} className="px-4 py-2.5 bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl font-bold flex items-center gap-2"> <Plus className="w-4 h-4" /> Add Transaction </button> </div> </div> ); } function TransactionsView() { const { transactions, deleteTransaction } = useApp(); return ( <div className="p-6 bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 space-y-4"> <h3 className="font-bold text-lg">Transaction History</h3> <div className="divide-y divide-slate-100 dark:divide-slate-800"> {transactions.map(t => ( <div key={t.id} className="py-3 flex justify-between items-center text-sm"> <div> <div className="font-bold">{t.groupName} ({t.paymentSourceName})</div> <div className="text-xs text-slate-400">{t.date} • {t.remarks || "No remarks"}</div> </div> <div className="flex items-center gap-4"> <span className={`font-extrabold ${t.type === "income" ? "text-emerald-500" : "text-rose-500"}`}> {t.type === "income" ? "+" : "-"} ?{t.amount.toLocaleString("en-IN")} </span> <button onClick={() => deleteTransaction(t.id!)} className="text-slate-400 hover:text-rose-500"><Trash2 className="w-4 h-4"/></button> </div> </div> ))} </div> </div> ); } function ReportsView() { const { transactions } = useApp(); const income = transactions.filter(t => t.type === "income").reduce((acc, t) => acc + t.amount, 0); const expense = transactions.filter(t => t.type === "expense").reduce((acc, t) => acc + t.amount, 0); return ( <div className="p-6 bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 space-y-4"> <h3 className="font-bold text-lg">Reports & Financial Summary</h3> <div className="p-4 bg-slate-50 dark:bg-slate-800/50 rounded-xl space-y-2 text-sm"> <div className="flex justify-between font-semibold"><span>Total Income Recorded:</span><span className="text-emerald-500">?{income.toLocaleString("en-IN")}</span></div> <div className="flex justify-between font-semibold"><span>Total Expenses Recorded:</span><span className="text-rose-500">?{expense.toLocaleString("en-IN")}</span></div> <div className="flex justify-between font-extrabold pt-2 border-t border-slate-200 dark:border-slate-700"><span>Net Balance:</span><span>?{(income - expense).toLocaleString("en-IN")}</span></div> </div> </div> ); } function MastersView() { const { masters } = useApp(); return ( <div className="p-6 bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 space-y-4"> <h3 className="font-bold text-lg">Master Configured Items</h3> <div className="grid grid-cols-2 sm:grid-cols-3 gap-3"> {masters.map(m => ( <div key={m.id} className="p-3 bg-slate-50 dark:bg-slate-800 rounded-xl text-xs font-semibold"> <div>{m.masterName}</div> <span className="text-[10px] text-slate-400">{m.masterType}</span> </div> ))} </div> </div> ); } function SettingsView() { const { userProfile, updateUserProfile } = useApp(); const [name, setName] = useState(userProfile.name); return ( <div className="p-6 bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 space-y-4 max-w-md"> <h3 className="font-bold text-lg">Profile Settings</h3> <div> <label className="block text-xs font-semibold text-slate-400 mb-1">Display Name</label> <input type="text" value={name} onChange={e => setName(e.target.value)} className="w-full px-3 py-2 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl text-sm" /> </div> <button onClick={() => updateUserProfile({ name })} className="px-4 py-2 bg-emerald-500 text-white font-bold rounded-xl text-sm">Save Changes</button> </div> ); } function QuickAddModal({ isOpen, onClose }: { isOpen: boolean; onClose: () => void }) { const { addTransaction } = useApp(); const [amount, setAmount] = useState(""); const [groupName, setGroupName] = useState("Groceries"); const [type, setType] = useState<TransactionType>("expense"); if (!isOpen) return null; const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); if (!amount) return; addTransaction({ type, date: new Date().toISOString().split("T")[0], groupId: "g1", groupName, paymentSourceId: "p1", paymentSourceName: "HDFC Bank", amount: parseFloat(amount), remarks: "Quick Entry" }); onClose(); }; return ( <div className="fixed inset-0 bg-slate-900/50 backdrop-blur-sm z-50 flex items-center justify-center p-4"> <div className="bg-white dark:bg-slate-900 p-6 rounded-2xl border border-slate-200 dark:border-slate-800 max-w-sm w-full space-y-4"> <div className="flex justify-between items-center"><h3 className="font-bold">Add Entry</h3><button onClick={onClose}><X className="w-5 h-5"/></button></div> <form onSubmit={handleSubmit} className="space-y-3"> <input type="number" placeholder="Amount (?)" value={amount} onChange={e => setAmount(e.target.value)} className="w-full px-3 py-2 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl text-sm" /> <button type="submit" className="w-full py-2 bg-emerald-500 text-white font-bold rounded-xl text-sm">Save</button> </form> </div> </div> ); }
