import React, { useState } from "react"; import { Plus } from "lucide-react"; import { useApp } from "../context/AppContext"; import { Card } from "../components/common/Card"; import { Button } from "../components/common/Button"; import { Modal } from "../components/common/Modal"; import { MasterTable } from "../components/masters/MasterTable"; import { DashboardSkeleton } from "../components/common/Loading"; import { MasterType } from "../types"; export function MastersPage() { const { expenseGroups, expenseSubGroups, incomeGroups, incomeSubGroups, paymentSources, addMasterItem, deleteMasterItem, userProfile, isDataLoading } = useApp(); const [activeTab, setActiveTab] = useState<MasterType>("EXPENSE_GROUP"); const [isModalOpen, setIsModalOpen] = useState(false); const [name, setName] = useState(""); const [description, setDescription] = useState(""); const [groupName, setGroupName] = useState(""); const [type, setType] = useState("Cash"); const [accountNumber, setAccountNumber] = useState(""); const openAddModal = () => { setName(""); setDescription(""); setGroupName(""); setType("Cash"); setAccountNumber(""); setIsModalOpen(true); }; const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); if (!name.trim()) return; if (activeTab === "EXPENSE_GROUP") { addMasterItem({ masterType: "EXPENSE_GROUP", masterName: name, name, description } as any); } else if (activeTab === "EXPENSE_SUBGROUP") { addMasterItem({ masterType: "EXPENSE_SUBGROUP", masterName: name, name, parentId: groupName, groupName: groupName } as any); } else if (activeTab === "INCOME_GROUP") { addMasterItem({ masterType: "INCOME_GROUP", masterName: name, name, description } as any); } else if (activeTab === "INCOME_SUBGROUP") { addMasterItem({ masterType: "INCOME_SUBGROUP", masterName: name, name, parentId: groupName, groupName: groupName } as any); } else if (activeTab === "PAYMENT_SOURCE") { addMasterItem({ masterType: "PAYMENT_SOURCE", masterName: name, name, type, accountNumber } as any); } setIsModalOpen(false); }; const tabs: { id: MasterType; label: string }[] = [ { id: "EXPENSE_GROUP", label: "Expense Groups" }, { id: "EXPENSE_SUBGROUP", label: "Expense Subgroups" }, { id: "INCOME_GROUP", label: "Income Groups" }, { id: "INCOME_SUBGROUP", label: "Income Subgroups" }, { id: "PAYMENT_SOURCE", label: "Payment Sources" }, ]; const currentItems = activeTab === "EXPENSE_GROUP" ? expenseGroups : activeTab === "EXPENSE_SUBGROUP" ? expenseSubGroups : activeTab === "INCOME_GROUP" ? incomeGroups : activeTab === "INCOME_SUBGROUP" ? incomeSubGroups : paymentSources; if (isDataLoading) return <DashboardSkeleton />; return ( <div className="space-y-6"> <div className="flex flex-wrap gap-2 p-1.5 bg-white dark:bg-slate-900 rounded-2xl border border-slate-200/80 dark:border-slate-800 shadow-sm"> {tabs.map(tab => ( <button key={tab.id} onClick={() => setActiveTab(tab.id)} className={`px-4 py-2 rounded-xl text-xs font-bold transition-all ${ activeTab === tab.id ? "bg-slate-900 dark:bg-slate-100 text-white dark:text-slate-900 shadow-md" : "text-slate-600 dark:text-slate-400 hover:text-slate-900 dark:hover:text-white" }`} > {tab.label} </button> ))} </div> <Card className="p-6"> <div className="flex items-center justify-between mb-6"> <div> <h3 className="font-bold text-slate-900 dark:text-white text-base">{tabs.find(t => t.id === activeTab)?.label}</h3> <p className="text-xs text-slate-500 dark:text-slate-400">Configured for your workspace</p> </div> {userProfile.role !== "Viewer" && ( <Button variant="primary" size="sm" icon={Plus} onClick={openAddModal}> Add Item </Button> )} </div> <MasterTable items={currentItems} onDelete={(id) => deleteMasterItem(id)} /> </Card> <Modal isOpen={isModalOpen} onClose={() => setIsModalOpen(false)} title="Add Master Item"> <form onSubmit={handleSubmit} className="space-y-4"> <div> <label className="block text-xs font-semibold text-slate-600 dark:text-slate-400 mb-1">Name *</label> <input type="text" required value={name} onChange={(e) => setName(e.target.value)} className="w-full px-3 py-2 bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl text-sm" /> </div> <div className="flex justify-end gap-2 pt-2"> <Button type="button" variant="outline" size="sm" onClick={() => setIsModalOpen(false)}>Cancel</Button> <Button type="submit" variant="primary" size="sm">Save Record</Button> </div> </form> </Modal> </div> ); }
